query userProfile($username: String!) {
  matchedUser(username: $username) {
    github
    twitter
    profile {
      ranking
      reputation
      userAvatar
      realName
      aboutMe
    }
    submitStats: submitStatsGlobal {
      acSubmissionNum {
        difficulty
        count
      }
    }
  }
}
